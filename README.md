# HomeAIO

This repository contains the HomeAIO project (M5Stack / PlatformIO + LVGL UI).

Build & upload (PlatformIO):

```powershell
pio run
pio run --target upload --upload-port COM9
```

Notes:
- This project uses PlatformIO and M5Unified.
- The UI is generated with EEZ Studio and LVGL.
